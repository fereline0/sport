<Window x:Class="frontend.Views.ProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:frontend.Views"
        mc:Ignorable="d"
        Title="{Binding Product.Name}" Height="450" Width="800">
    <Grid>
        <Border Padding="10">
            <StackPanel>
                <Image Source="{Binding Product.Image}" Height="100" Stretch="Uniform" />
                <TextBlock Text="{Binding Product.Name}" TextWrapping="Wrap" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="{Binding Product.Description}" TextWrapping="Wrap" Foreground="DimGray"/>
                <TextBlock Text="{Binding Product.Price, StringFormat='{}{0} ₽'}" FontWeight="Bold"/>
                <StackPanel>
                    <Button Margin="0 5" Command="{Binding DataContext.AddToOrderCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Product}">Добавить в заказ</Button>
                    <Button Margin="0 0 0 5" Command="{Binding DataContext.RemoveFromOrderCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Product}">Удалить из заказа</Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
